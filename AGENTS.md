## Cursor Cloud specific instructions

### Repository State

This repository ("SmartOrder") is currently a skeleton project with only a `README.md`. No source code, dependencies, build tools, or services exist yet. The README (in Greek) describes an intended order management system but nothing has been implemented.

### Development Environment

- No package manager lockfile or dependency manifest exists (`package.json`, `requirements.txt`, etc.)
- No `src/`, `public/`, or other source directories exist
- No lint, test, or build commands are available
- No services to start

### When Code Is Added

Once source code is introduced, update this section with:
- How to install dependencies
- How to run lint/test/build
- How to start development servers
- Any non-obvious environment requirements
